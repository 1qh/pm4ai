{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "test": {
      "cache": false,
      "env": ["CI", "PM4AI_STATE_DIR", "NEXT_DIST_DIR"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "check": {
      "dependsOn": ["build"],
      "cache": false
    },
    "fix": {
      "dependsOn": ["build"],
      "cache": false
    }
  }
}
