{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "test": {
      "cache": false,
      "env": ["CI", "PM4AI_SOCKET_DIR"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "check": {
      "dependsOn": ["build"],
      "cache": false
    },
    "fix": {
      "dependsOn": ["build"],
      "cache": false
    }
  }
}
